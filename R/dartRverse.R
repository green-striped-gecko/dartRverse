#' dartRverse-package
#'
#' The 'dartRverse' combines a set of packages that work together to analyse SNP and silicodart data. 
#' It is the successor of the package dartR. Due to its high number of dependencies it was necessary to break the package into several, smaller packages to reduce overall dependencies from other packages and to speed up testing.
#' You can check which packages do exist by typing \code{dartRverse_install()}.
#'
#' Loading the package attaches all installed dartRverse packages and prints which are installed, which are not, and which are installed but failed to load. Set \code{options(dartRverse.quiet = TRUE)} before \code{library(dartRverse)} to suppress this startup output.
#' @keywords internal
"_PACKAGE"

